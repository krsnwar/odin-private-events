<nav class="navbar navbar-expand-lg navbar-dark bg-success">
  <div class="container d-flex">
    <a class="navbar-brand" href="#">Private Events!</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" aria-current="page" href="<%= events_path %>">All Events</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Event Chosen</a>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <a class="nav-link disabled"><%= current_user.email %></a>
          </li>
        <% end %>
      </ul>
    </div>
    <div>
      <% if user_signed_in? %>
        <%= button_to "Sign Out", destroy_user_session_path, :method => :delete, class: "btn btn-light d-flex" %>
      <% else %>
        <%= link_to "Sign In", new_user_session_path, class: "btn btn-light d-flex" %>
      <% end %>
    </div>
  </div>
</nav>
